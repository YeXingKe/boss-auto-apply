# AI 面试学习路线与项目话术

适用定位：`5年 前端 + AI 应用工程化 + AI Agent + RAG + Agent + go/python`。

这份资料优先解决三个问题：

1. 面试前到底按什么顺序学。
2. BM25、HNSW、混合召回、RAG、Agent、Python/Go 编排、前端流式怎么讲。
3. 项目中遇到的困难怎么回答得像真实做过。

## 0. 一句话定位

面试时先把自己钉在这个位置：

> 我不是算法训练方向。我的定位是 **5 年前端 + AI 应用工程化**：能把 RAG、Agent、LLM 做成可上线的产品和链路。前端负责对话/质检/复核体验与流式交互；Go/Python 负责编排、检索、任务与模型调用；重点解决检索准确、工具安全、流式稳定、结果可解释、成本可控和线上可观测。

不要说自己训练大模型，也不要把 RAG 说成“AI 长期知识库”。要说：

> RAG 是检索增强链路，知识库只是它的数据来源之一。

## 1. 总学习路线

### 第一阶段：先把项目讲顺

必须能 2 分钟说清：

```text
业务背景
-> 为什么人工质检不够
-> ASR 转文本
-> RAG 召回质检规则
-> LLM 做结构化判断
-> 前端复核台 + Python/Go 任务状态、重试、入库、查询
-> 难点是准确、稳定、可解释、可追踪
```

标准讲法：

> 我做的是金融客服场景下的 AI 质检平台。系统先把通话音频通过 ASR 转成文本，再把对话片段拿去 RAG 检索质检规则、话术规范和业务制度，然后交给 LLM 做结构化判断，输出是否违规、命中规则、证据片段、置信度和原因。  
> 我主要负责 **前端 + AI 应用工程化**：前端做质检结果列表、证据高亮、人工复核台、Prompt/规则版本对比和流式调试面板；工程侧参与 RAG 链路（chunk、混合召回、rerank）、Agent 工具 schema、模型调用封装（超时/重试/结构化 JSON）、任务状态与 badcase 回流。后端编排以 **Python FastAPI + 任务队列** 为主，部分高并发接口用 **Go** 做网关和限流。

### 第二阶段：补 RAG 检索知识

按这个顺序学：

```text
文档解析
-> chunk 切分
-> embedding
-> 向量召回
-> BM25 关键词召回
-> 混合召回/双向召回
-> rerank 重排序
-> Prompt 组装
-> 结构化输出
-> 评测集和 badcase
```

面试官问 RAG，先别讲框架，先讲链路。框架只是实现手段。

### 第三阶段：补 AI 应用框架（按岗位侧重）

**Python（RAG / Agent 编排主战场）**

- LangChain / LangGraph：链式编排、Agent 状态机、工具调用、RAG retriever。
- LlamaIndex：文档索引、chunk、混合检索、query engine。
- FastAPI + Pydantic：结构化入参/出参、OpenAPI、与前端 TypeScript 类型对齐。

**Go（高并发与平台化）**

- Gin/Fiber：AI 网关、鉴权、限流、SSE 代理。
- 任务调度、MQ 消费、模型调用超时与熔断。

**前端（差异化优势）**

- 流式输出：SSE / fetch stream / WebSocket，打字机效果与中断重试。
- 对话 UI：多轮上下文、工具调用过程可视化、citation 来源展示。
- 运营台：badcase 标注、召回结果 debug、Prompt A/B、阈值配置。

生产里不要让前端或业务代码到处直接调模型 API，要经 **BFF/AI Service** 统一做超时、重试、日志、成本统计和降级。

### 第四阶段：补项目难点

面试最容易深挖这几类：

- RAG 召回不准怎么办。
- BM25 和向量召回有什么区别。
- HNSW 是什么，为什么向量库要用近似最近邻。
- LLM 输出 JSON 不稳定怎么办。
- 模型超时和失败怎么处理。
- 质检误判率升高怎么排查。
- Agent 工具调用怎么做权限和安全。
- Spring AI / LangChain4j 在项目里怎么用。

## 2. RAG 核心链路

### RAG 是什么

RAG 是 Retrieval-Augmented Generation，意思是先从外部知识库检索相关资料，再把资料作为上下文交给大模型回答或判断。

质检项目里可以这样讲：

> 质检规则、产品制度、话术规范都是企业内部知识，大模型本身不知道。如果直接把 ASR 文本丢给模型，它可能凭通用知识判断，容易幻觉。所以我们先召回相关规则，再让模型基于规则和通话片段做判断。

### RAG 链路

```text
文档上传
-> 解析清洗
-> chunk 切分
-> embedding 向量化
-> 存向量、原文、元数据
-> 查询文本向量化
-> 向量召回 + BM25 关键词召回
-> 合并去重
-> rerank 重排序
-> topN 进入 Prompt
-> LLM 结构化输出
-> 证据、规则 ID、模型响应入库
```

### RAG 评估指标

必背：

```text
召回率：该召回的规则有没有召回
TopK 命中率：正确规则是否在 top3/top5
MRR：正确结果排名是否靠前
误召回率：无关规则是不是太多
答案正确率：最终判断是否正确
幻觉率：没有证据时是否乱答
延迟和成本：检索、重排、模型调用耗时和 token 成本
```

## 3. BM25

### BM25 是什么

BM25 是一种传统关键词检索算法，可以理解为“更聪明的关键词匹配”。它会看 query 里的词在文档里出现的频率，也会考虑这个词是不是有区分度。

面试短答：

> BM25 偏关键词召回，适合精确词、专有名词、规则编号、产品名、敏感词这类场景。它不理解语义，但对明确词命中很稳定。

### BM25 看哪几个因素

不用背公式，背三个点：

```text
词频 TF：词在当前文档里出现越多，相关性越高，但不是无限增加。
逆文档频率 IDF：越少见的词区分度越高，比如规则编号、产品名。
文档长度归一化：避免长文档因为词多而天然占便宜。
```

### BM25 的优点

- 对精确关键词稳定。
- 对规则编号、敏感词、产品名效果好。
- 不依赖 embedding 模型。
- 可解释性强，知道是哪个词命中了。

### BM25 的缺点

- 不理解同义表达。
- 对口语、错别字、ASR 错字不友好。
- 对“隐晦承诺”“情绪风险”这类语义问题不够强。

### 项目里怎么讲

> 在质检项目里，BM25 可以用来做关键词补召回。比如规则编号、产品名称、敏感词、黑名单词、固定话术，这些靠 BM25 很稳定。向量召回更适合同义表达和语义相似，比如客户说法和规则描述不完全一样时。我们会把 BM25 和向量召回结果合并，再做去重和重排序。

## 4. HNSW

### HNSW 是什么

HNSW 是 Hierarchical Navigable Small World，是向量数据库里常见的近似最近邻检索算法。它不是精确暴力扫描所有向量，而是构建多层图结构，让查询可以从高层快速跳到低层附近区域，再找到相似向量。

面试短答：

> HNSW 是向量检索常用索引结构，用多层图加速近似最近邻搜索。它牺牲一点点精确度，换取很大的查询性能提升，适合大规模 embedding 检索。

### 为什么需要 HNSW

如果知识库有几十万、几百万 chunk，每次 query 都和所有向量算余弦相似度，成本太高。HNSW 的价值是减少比较次数。

### HNSW 关键词

```text
ANN：近似最近邻
多层图：上层稀疏、下层稠密
召回率和性能权衡
efSearch：查询时搜索范围，越大越准但越慢
M：图中连接数，影响索引大小和召回效果
```

### 项目里怎么讲

> 我不把 HNSW 当算法岗去展开公式。我在项目里的理解是，向量库要解决的是 topK 相似 chunk 快速召回。如果数据量小，可以直接用 ES dense vector 或 PGVector 降低复杂度；如果向量规模变大，就需要 HNSW 这种 ANN 索引来提升检索性能。调参上主要是在召回率、延迟和内存之间做平衡。

## 5. 双向召回 / 混合召回

### 先纠正概念

面试里更常见的说法是“混合召回”或“多路召回”。你说“双向召回”时，可以解释成：

> 我这里说的双向召回，主要是关键词召回和向量语义召回两路一起做，也可以理解为 hybrid search。

不要硬造概念。

### 为什么要双向召回

单靠向量召回的问题：

- 专有名词可能不准。
- 规则编号、产品名、金额、日期这类精确信息可能被弱化。
- embedding 模型对 ASR 错字、短 query 不稳定。

单靠 BM25 的问题：

- 不理解同义表达。
- 规则写法和客户说法不一致时容易漏。
- 对上下文语义判断弱。

所以要两路召回：

```text
BM25 召回：抓精确词、敏感词、规则编号、产品名。
向量召回：抓语义相似、同义表达、隐晦表达。
合并去重：按 documentId/chunkId 去重。
分数融合：按权重、RRF 或归一化分数融合。
Rerank：再用重排模型或规则做最终排序。
```

### 面试标准回答

> 我们不会只依赖一种召回。BM25 适合精确关键词，比如敏感词、规则编号、产品名；向量召回适合语义相似，比如客户用了同义表达或隐晦表达。实际链路里可以先做两路召回，分别取 topK，然后按 chunkId 去重，再用分数融合或 RRF 排序，最后 rerank 取前几个进入 Prompt。这样能降低漏召回，也能控制无关内容进入上下文。

### RRF 是什么

RRF 是 Reciprocal Rank Fusion，用排名融合多路召回结果。它不强依赖不同检索器的原始分数可比性，更适合 BM25 分数和向量相似度这种不同体系。

面试短答：

> RRF 是一种按排名融合的方式。不同召回通道的分数尺度不一样，BM25 分数和向量相似度不好直接相加，所以可以按排名做融合，排名越靠前贡献越大。

## 6. Rerank 重排序

### 为什么需要 rerank

第一阶段召回一般追求“别漏”，所以 topK 会稍宽，比如每路召回 20-50 条。但这些结果里会有噪声，需要第二阶段重排序。

标准回答：

> 召回阶段更看重召回率，宁愿多捞一点；rerank 阶段更看重精度，把最相关的内容排到前面。最后进入 Prompt 的通常只有 5-8 个 chunk，否则 token 成本高，还会干扰模型。

### 项目里怎么讲

> 质检场景更怕漏掉关键规则，所以第一阶段会稍微放宽召回，再通过阈值、元数据过滤和 rerank 控制噪声。如果正确规则没召回，是召回问题；如果召回了但排很后，是排序问题；如果规则在前面但模型还判错，才看 Prompt 和模型。

## 7. Python / Go AI 编排（原 Java 框架章节）

### Python 侧怎么讲

面试短答：

> Python 更适合 RAG 脚本、embedding 批处理、Agent 原型和评测流水线。我会用 FastAPI 暴露检索和对话接口，Pydantic 约束结构化输出，Celery/Redis 做异步质检任务，LangChain 或 LangGraph 管工具调用和状态，但生产关键链路（日志、重试、权限）仍要自己可控。

核心概念：

```text
Retriever / VectorStore：RAG 检索
Tool / Function Calling：Agent 工具
Structured Output：Pydantic / JSON Schema
Streaming：SSE 流式返回给前端
Callback / Trace：LangSmith 或自研 traceId
```

### Go 侧怎么讲

> Go 适合 AI 网关和高并发场景：统一鉴权、限流、SSE 代理、模型路由、熔断降级。业务编排若已在 Python，Go 可以只做边缘接入，避免重复实现 RAG 逻辑。

### 前端怎么接

> 前端不直连模型 Key。通过 BFF 调 `/chat/stream`、`/rag/debug`、`/task/{id}`；流式用 EventSource 或 fetch reader；展示 citation、工具调用步骤和置信度；复核台支持证据跳转和 badcase 一键标注。

### 和「纯后端 AI 岗」的差异

> 我能讲清 RAG/Agent 链路，也能落地 **对话产品体验** 和 **工程观测**。很多团队缺的是「模型能跑 + 运营能用 + 出问题能查」，这正是前端 + 工程化的组合价值。

## 8. 项目困难怎么讲

### 难点 1：RAG 召回不准

标准回答：

> 这个项目里一个难点是 RAG 召回不准。因为 ASR 文本是口语化的，里面有停顿、重复、错字，而质检规则是比较正式的制度文本，两边表达不一致。单靠向量召回可能漏掉规则编号、敏感词；单靠关键词又抓不到隐晦表达。  
> 我们的处理思路是先拆 badcase，看正确规则有没有被召回。如果没召回，就优化 chunk、降低阈值、增大 topK、加 BM25 关键词补召回和同义词；如果召回了但排名靠后，就加 rerank；如果规则已经召回但模型没判对，就改 Prompt 和结构化输出要求。

一句话：

> 先判断是没召回、排序差，还是模型判断错。

### 难点 2：ASR 错字影响质检

标准回答：

> ASR 错字会影响关键词和向量召回。比如产品名、敏感词、金额、时间说错或转写错，检索就可能偏。我们会做文本清洗、业务热词、同义词和规则词典，也会避免只靠单个关键词判断。对低置信度 ASR 或关键风险项，会进入人工复核。

### 难点 3：LLM 输出不稳定

标准回答：

> 模型有时会输出不合法 JSON，或者字段名不稳定。生产里不能直接相信自然语言输出，所以我们要求结构化 JSON，字段包括 rule_id、hit、evidence、confidence、reason。后端会做 JSON Schema 或 DTO 校验，解析失败可以重试一次或切兜底 Prompt，多次失败转人工或规则兜底。

### 难点 4：模型调用超时和失败

标准回答：

> LLM 调用必须设置超时，不能拖垮业务线程。我们会记录 ai_task，包括 taskId、模型名、Prompt 版本、状态、耗时、错误码、重试次数。失败后进入 RETRYING，由定时任务或延迟队列补偿，超过最大重试次数后进入 FAILED 或 REVIEW_REQUIRED。敏感词这类强规则可以走规则兜底。

### 难点 5：质检结论要可解释

标准回答：

> AI 质检不能只给一个“违规/不违规”，否则业务不信，也没法复核。我们会保存命中的规则 ID、规则版本、证据片段、模型原始响应、置信度、Prompt 版本和人工复核结果。这样误判时可以回看是规则没召回、模型误判，还是后端解析入库问题。

### 难点 6：误判率突然升高

标准回答：

> 我会先看是全局升高还是某类规则、某个业务线升高；然后抽 badcase，看是 ASR、召回、rerank、Prompt、模型版本还是后端解析问题；再检查最近变更，比如规则库、Prompt、embedding、阈值、模型版本、代码发布。先止损，比如回滚 Prompt 或规则版本、切旧模型、低置信度转人工；修复后把 badcase 加入评测集回归。

记忆：

```text
指标 -> badcase -> 最近变更 -> 分层排查 -> 止损 -> 回归
```

## 10. 面试高频问答

### Q1：BM25 和向量召回区别？

> BM25 是关键词召回，适合精确词、专有名词、规则编号、敏感词，稳定且可解释，但不理解语义。向量召回是语义召回，适合同义表达和上下文相似，但对专有名词、短 query、ASR 错字可能不稳定。生产里通常做混合召回，再合并去重和 rerank。

### Q2：为什么不用纯向量召回？

> 纯向量召回可能漏掉精确信息，比如规则编号、产品名、敏感词、金额。质检场景里这些词很关键，所以会加 BM25 或规则匹配补召回。

### Q3：为什么不用纯 BM25？

> 纯 BM25 不理解同义表达。客服和客户说话很口语化，不一定和规则文本用同样的词。比如违规承诺可能有很多隐晦说法，这时向量召回和 LLM 语义判断更有价值。

### Q4：HNSW 为什么是近似，不做精确？

> 精确搜索要和所有向量计算相似度，数据量大时延迟和成本高。HNSW 用多层图做近似最近邻，牺牲少量精度换查询性能。实际工程里会通过 efSearch、topK 和 rerank 把召回率拉回来。

### Q5：topK 怎么选？

> topK 太小容易漏召回，太大噪声多、token 成本高。一般第一阶段召回可以取 20-50，rerank 后取 5-8 个进入 Prompt。最终靠评测集调，不是拍脑袋。

### Q6：相似度阈值怎么定？

> 先准备标注集，看不同阈值下召回率、误召回率、topK 命中率和最终误判率。阈值太高漏召回，太低噪声多。我们之前会结合 badcase 和测试样本调，比如从 0.86 往下试 0.82、0.8，但不会盲目调低。

### Q7：RAG 正确规则召回了，但模型还是误判怎么办？

> 这时重点不是召回，而是 Prompt、输出格式和模型判断。可以强化判断标准，要求模型引用证据片段，输出结构化 JSON；对低置信度转人工；也可以加入 few-shot 示例或二次校验。关键是先确认正确规则已经在上下文里。

### Q8：LangChain / LangGraph 和自研编排怎么选？

> 快速验证和 Agent 状态机用 LangGraph/LangChain 效率高；生产里关键链路（权限、审计、结构化输出、成本）我会封装在 FastAPI/Go 服务里，框架只负责编排，不替代业务治理。团队若以 Java 为主，可提 LangChain4j/Spring AI 作对比，但我的主力栈是 Python + 前端。

### Q9：Agent 工具调用怎么保证安全？

> 工具必须白名单化，每个工具有名称、描述、参数 schema、权限级别和超时时间。模型传参后，后端要做 DTO/JSON Schema 校验、业务校验和权限校验。只读工具可以自动执行，写操作要二次确认或人工审核。还要限制最大调用次数、最大耗时，并记录审计日志。

### Q10：怎么证明你不是只会调 API？

> 我会从全链路讲：前端复核台与流式体验；Python/Go 侧任务状态、MQ、幂等、补偿；RAG 混合召回与 rerank；Prompt 版本与结构化 JSON；Agent 工具 schema 与权限；人工复核与 badcase 回流；日志 trace 与 token 成本。调 API 只是一环，上线要解决体验、稳定性、可解释性和业务闭环。

## 11. 七天突击安排

### Day 1：项目主线

- 背 30 秒自我介绍（前端 + AI 工程化）。
- 背 2 分钟 AI 质检项目（含前端复核台）。
- 背「我不是算法训练，我是 AI 应用工程化 + 前端落地」。

### Day 2：RAG 基础

- RAG 链路。
- chunk、overlap、embedding、topK、阈值。
- RAG 回答错怎么排查。

### Day 3：BM25 + HNSW + 混合召回

- BM25 适合关键词。
- HNSW 适合大规模向量近似检索。
- 双向召回讲成 BM25 + 向量召回。
- RRF、rerank、合并去重。

### Day 4：Python/Go 编排 + 前端流式

- Python：FastAPI、Pydantic、LangChain/LangGraph、Celery。
- Go：网关、限流、SSE 代理（按项目选讲）。
- 前端：SSE 流式、citation、工具调用可视化、debug 面板。

### Day 5：AI 工程化

- LLM 超时失败。
- JSON 输出不稳定。
- Prompt 版本管理。
- Token 成本控制。
- 日志、指标、trace。

### Day 6：项目困难

背 6 个难点：

```text
RAG 召回不准
ASR 错字
LLM JSON 不稳定
模型超时失败
质检可解释
误判率升高
```

### Day 7：模拟面试

按这 10 题练：

1. 介绍你的 AI 质检项目。
2. RAG 是什么，为什么不用直接问模型？
3. BM25 和向量召回区别？
4. HNSW 是什么？
5. 双向召回怎么做？
6. topK 和阈值怎么调？
7. LangChain/LangGraph 和自研怎么选？
8. LLM 输出 JSON 不稳定怎么办？
9. 误判率突然升高怎么排查？
10. 怎么证明你不是只调 API？

## 12. 最后背诵版

你最后至少要背熟这一段：

> 我做 AI 应用落地时，会把链路拆开：前端负责流式对话、证据展示和复核；Python/Go 负责 ASR 任务、RAG 检索、模型调用与状态机；知识侧有 chunk、embedding、混合召回和 rerank；模型侧要求结构化 JSON；工程侧有 trace、成本、降级和 badcase。效果不好时，先看规则有没有召回，再查排序和 Prompt，而不是只换模型。

